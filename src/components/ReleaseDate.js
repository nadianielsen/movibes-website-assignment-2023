import TextStyle from "./TextStyle";

const ReleaseDate = ({release_date}) => {


    return (
        <div>
            <TextStyle text={release_date} />
        </div>
    );
}

export default ReleaseDate;
import TextStyle from "./TextStyle";

const MovieTitleDetail = ({title}) => {

    return (
        <div>
            <TextStyle text={title} />
        </div>
    );
}

export default MovieTitleDetail;